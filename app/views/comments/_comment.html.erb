<%= form_for ([review , comment]), remote: true, html:{id: "new_comment_of_review_#{review.id}"} do |f| %>
  <%= current_user.email %>
  <%= f.label :body, "REPLY:" %>
  <br>
  <%= f.text_area :body %>
  <br>
  <%= f.submit "Submit Reply" ,class: 'btn btn-primary btn-lg' %>
  <% end %>
